- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim/spell/de.utf-8.add: de.utf-8.add
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
